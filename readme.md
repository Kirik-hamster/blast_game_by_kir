# Blast Game Prototype (Test Assignment)

Прототип игры в жанре "Blast", выполненный на движке Cocos Creator. 

---
## 📂 Структура проекта

Основная логика игры распределена по специализированным сервисам для обеспечения чистоты кода и легкой масштабируемости.

```text
assets/
├── Scripts/                # Игровая логика
│   ├── GameController.ts   # Управляет состоянием, UI и связями
│   ├── GridModel.ts        # Математическая модель поля (данные сетки)
│   ├── GameConfig.ts       # Глобальные настройки баланса и таймингов
│   ├── BonusService.ts     # Сервис для фазы (финальные бонусы)
│   ├── HintService.ts      # Система поиска и визуализации подсказок при бездействии
│   └── BoosterService.ts   # Расчет зон поражения для ракет и бомб
├── Scenes/                 # Игровые сцены (MainScene.fire)
└── Textures/               # Спрайты кубиков, бустеров и элементов интерфейса

```
---

## 🛠 Ключевые системы и сервисы

### 🧩 HintService (Система подсказок)

Автоматически отслеживает активность игрока. Если в течение времени, заданного в `HINT_DELAY`, не совершается ход, сервис находит случайную доступную группу кубиков и запускает их пульсацию. Любое взаимодействие с полем сбрасывает таймер.

### 🎇 BonusService (Fever Mode)

Активируется при достижении `TARGET_SCORE`. Сервис последовательно:

1. Конвертирует оставшиеся ходы в случайные бустеры (ракеты/бомбы).
2. Поочередно взрывает их с задержкой, создавая эффект финального праздника.
3. Позволяет игроку набрать бонусные очки сверх лимита перед показом финального экрана.

### ⚙️ GameConfig

Централизованный файл настроек, позволяющий менять баланс уровня без изменения кода:

* **Баланс**: Количество ходов, цель по очкам, пороги появления бустеров.
* **Тайминги**: Скорость падения кубиков, интервалы взрывов и задержки UI.
* **Лимиты**: Ограничения на использование вспомогательных инструментов (телепорт, шаффл).

---

## 🎮 Геймплейные особенности

* **Цепные реакции**: Бустеры могут активировать друг друга, создавая масштабные взрывы.
* **Screen Shake**: Динамическая тряска камеры и элементов интерфейса при мощных взрывах (интенсивность зависит от типа бустера).

## 🚀 Реализованные дополнительные задания
1. **Авто-перемешивание**: автоматический перемешивание поля при отсутствии ходов (лимит: 3 раза).
2. **Бустер "Бомба"**: создание бомбы в случайном месте на поле.
3. **Бустер "Телепорт"**: тактический обмен двух любых тайлов местами с визуальным подтверждением выбора.
4. **Супер-тайлы**: появление Ракет (уничтожение линий) и Бомб (взрыв в радиусе) при сожжении больших групп тайлов.
5. **Juicy UX**: анимация пульсации при каждом клике, динамическая подсветка активных бустеров и блокировка кнопок при нулевом заряде.

---

### Как запустить

1. Склонируйте репозиторий.
2. Откройте проект в **Cocos Creator 2.4.x**.
3. Запустите сцену `MainScene`.

## 🎮 Демо игры
**[[ССЫЛКА НА МОЮ БЛАСТ ИГРУ](https://kirik-hamster.github.io/blast_game_by_kir/)]**

## 🛠 Технологии
- **Движок:** Cocos Creator 2.4.15
- **Язык:** TypeScript